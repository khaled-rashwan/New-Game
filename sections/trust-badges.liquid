{{ 'section-trust-badges.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .trust-badges-{{ section.id }} {
    background-color: {{ section.settings.background_color }};
  }

  .trust-badges-{{ section.id }} .trust-badge__title {
    color: {{ section.settings.title_color }};
    font-size: {{ section.settings.title_size }}px;
  }

  .trust-badges-{{ section.id }} .trust-badge__text {
    color: {{ section.settings.text_color }};
    font-size: {{ section.settings.text_size }}px;
  }

  .trust-badges-{{ section.id }} .trust-badge__icon {
    color: {{ section.settings.icon_color }};
    font-size: {{ section.settings.icon_size }}px;
  }
{%- endstyle -%}

<section class="trust-badges trust-badges-{{ section.id }} section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="trust-badges__grid">
      {%- for block in section.blocks -%}
        <div class="trust-badge" {{ block.shopify_attributes }}>
          {%- if block.settings.icon != blank -%}
            <div class="trust-badge__icon">
              {{ block.settings.icon }}
            </div>
          {%- endif -%}
          {%- if block.settings.title != blank -%}
            <h3 class="trust-badge__title">
              {{ block.settings.title }}
            </h3>
          {%- endif -%}
          {%- if block.settings.text != blank -%}
            <p class="trust-badge__text">
              {{ block.settings.text }}
            </p>
          {%- endif -%}
        </div>
      {%- endfor -%}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Trust Badges",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background Color",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "icon_color",
      "label": "Icon Color",
      "default": "#4CAF50"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Title Color",
      "default": "#000000"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text Color",
      "default": "#666666"
    },
    {
      "type": "header",
      "content": "Typography"
    },
    {
      "type": "range",
      "id": "icon_size",
      "min": 20,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Icon Size",
      "default": 32
    },
    {
      "type": "range",
      "id": "title_size",
      "min": 12,
      "max": 28,
      "step": 1,
      "unit": "px",
      "label": "Title Size",
      "default": 16
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 10,
      "max": 20,
      "step": 1,
      "unit": "px",
      "label": "Text Size",
      "default": 13
    },
    {
      "type": "header",
      "content": "Section Padding"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 40
    }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Trust Badge",
      "settings": [
        {
          "type": "text",
          "id": "icon",
          "label": "Icon (SVG or Unicode)",
          "default": "🔒"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Secure Shopping"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "SSL Protected"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Trust Badges",
      "blocks": [
        {
          "type": "badge",
          "settings": {
            "icon": "🔒",
            "title": "Secure Shopping",
            "text": "SSL Protected"
          }
        },
        {
          "type": "badge",
          "settings": {
            "icon": "🚚",
            "title": "Free Shipping",
            "text": "Orders over $75"
          }
        },
        {
          "type": "badge",
          "settings": {
            "icon": "↩️",
            "title": "Easy Returns",
            "text": "30-day policy"
          }
        },
        {
          "type": "badge",
          "settings": {
            "icon": "⭐",
            "title": "5-Star Reviews",
            "text": "1000+ happy customers"
          }
        }
      ]
    }
  ]
}
{% endschema %}
